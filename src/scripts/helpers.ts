

import {  get } from 'lodash';
// import { useSelector } from 'react-redux';
// export const getRedux = (path: any, defaultValue: any) => useSelector((state: any) => get(state, path, defaultValue));